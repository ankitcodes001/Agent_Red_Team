"""Agent Red Team — offensive-security agent for indirect prompt injection.

Public entry point is the ``redteam`` CLI (see ``cli.py``) and the
``Orchestrator`` (see ``orchestrator.py``).
"""

__version__ = "0.1.0.dev0"
